{
  "name": "forge",
  "version": "1.0.0",
  "private": true,
  "description": "Static newsletter preview and preflight studio for Prime Ingredients.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
